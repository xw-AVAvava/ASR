# GitHub Repository URL

Final GitHub repository URL:

```text
https://github.com/xw-AVAvava/ASR
```

The repository should include:

- source code
- README
- contribution file
- experiment outputs or screenshots when selected manually
- clear commit history from multiple team members

Large local model files under `models/` and generated experiment outputs under `outputs/` are intentionally excluded from Git.
